{
  "name": "@tags/models",
  "version": "1.0.0",
  "private": true,
  "description": "Shared data models and schemas for Tags monorepo",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "zod",
    "types",
    "schemas",
    "models",
    "monorepo"
  ],
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@tags/eslint-config": "workspace:*",
    "@tags/tsconfig": "workspace:*",
    "typescript": "^5.3.0"
  }
}
